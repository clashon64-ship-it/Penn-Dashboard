# Base URL of the backend proxy that reads the private Google Sheet.
# Leave blank to run against built-in sample data.
#
# Start the proxy from ./server (see server/README.md), then set this to its URL:
VITE_API_BASE_URL=http://localhost:8787
