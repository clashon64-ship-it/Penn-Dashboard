# Base URL of the analytics API the dashboard reads from.
# Leave blank to run against built-in sample data.
VITE_API_BASE_URL=

# Optional bearer token sent as `Authorization: Bearer <token>`.
VITE_API_TOKEN=
