# Production build: the frontend and the serverless proxy are deployed together
# on Vercel, so the API lives at the same origin under /api. This makes the app
# fetch GET /api/dashboard with no CORS and no hardcoded host.
VITE_API_BASE_URL=/api
